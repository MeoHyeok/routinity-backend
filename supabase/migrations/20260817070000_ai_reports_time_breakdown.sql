-- iOS 리포트 화면에 "하루를 어떻게 보냈는지" 차트를 그리기 위해, 리포트
-- 본문(content, 텍스트)과 별개로 구조화된 숫자를 함께 저장한다.
--
-- 생성 시점에 함께 저장해서 캐시로 재조회해도(cached: true) 그날 텍스트가
-- 서술하는 숫자와 항상 일치하게 한다 — 캐시 히트마다 다시 계산해서 매번
-- 다른 값을 돌려주면 텍스트와 차트가 어긋날 수 있다. 목표/기록이 없어
-- 시간 분배를 계산할 수 없었던 리포트는 null.
alter table ai_reports add column time_breakdown jsonb;
